<UserControl x:Class="Root_Siltron.NavigationPanelUI.RecipeWizardUI.RecipeWizardPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
           d:DesignHeight="818" d:DesignWidth="1912">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="12*"/>
            <ColumnDefinition Width="88*"/>
        </Grid.ColumnDefinitions>
        <Border Background="#FFCFCFCF"
                        BorderBrush="DimGray" BorderThickness="0,0,1,0">
            <Grid>
                <StackPanel>
                    <ToggleButton Content="Recipe Summary"
                                  IsChecked="True"
                            Command="{Binding btnWizardSummary}"
                            FontSize="15"
                            FontWeight="Bold"
                            Margin="10"
                            Height="40"/>
                    <Button Content="Alignment"
                            Command="{Binding btnWizardAlignnment}"
                            FontSize="15"
                            FontWeight="Bold"
                            Margin="10"
                            Height="40"/>
                    <Button Content="General"
                            Command="{Binding btnWizardGeneral}"
                            FontSize="15"
                            FontWeight="Bold"
                            Margin="10"
                            Height="40"/>

                </StackPanel>
                <Button Content="Back"
                        Command="{Binding btnWizardBack}"
                        FontSize="15"
                        VerticalAlignment="Bottom"
                        FontWeight="Bold"
                        Margin="10"
                        Height="40"/>
            </Grid>
        </Border>
        <Grid Grid.Column="1" x:Name="SubPanel" Background="White"/>


    </Grid>
</UserControl>
