<UserControl x:Class="RootTools.Printer.SRP350_UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RootTools.Printer"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">
    <StackPanel Orientation="Vertical" Width="200" HorizontalAlignment="Left">
        <ComboBox x:Name="comboPrinter" SelectedValue="{Binding p_sPrinter}" MinWidth="60" Margin="4"/>
        <CheckBox Content="Connect" IsChecked="{Binding p_bConnect}" Margin="4" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Button x:Name="buttonCut" Content="Cut" Margin="4" MinWidth="60" IsEnabled="{Binding p_bConnect}" Click="buttonCut_Click"/>
            <CheckBox Content="Feeding" IsChecked="{Binding p_bCutFeeding}" Margin="4" VerticalAlignment="Center"/>
        </StackPanel>
        <Button x:Name="buttonWrite" Content="Test Print" Margin="4" IsEnabled="{Binding p_bConnect}" Click="buttonWrite_Click"/>
    </StackPanel>
</UserControl>
