<UserControl x:Class="Root_LogViewer.LogViewer_UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_LogViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Beige">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="{Binding p_sPath}"/>
            <Label Grid.Column="1" Content="{Binding p_sInfo}" Background="LightPink"/>
        </Grid>

        <Menu Grid.Row="1" Background="AliceBlue">
            <Button x:Name="buttonOpen" Content="File Open" Click="buttonOpen_Click"/>
        </Menu>
        <TabControl Grid.Row="2" x:Name="tabLogGroup" Margin="2"/>
    </Grid>
</UserControl>
