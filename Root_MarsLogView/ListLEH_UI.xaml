<UserControl x:Class="Root_MarsLogView.ListLEH_UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_MarsLogView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid x:Name="dataGrid" IsReadOnly="True" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Module ID" Binding="{Binding p_sModule}"/>
            <DataGridTextColumn Header="Lot ID" Binding="{Binding p_sLot}"/>
            <DataGridTextColumn Header="Recipe Name" Binding="{Binding p_sRecipe}"/>
            <DataGridTextColumn Header="Flow Info" Binding="{Binding p_sFlowInfo}"/>
            <DataGridTextColumn Header="Event" Binding="{Binding p_sEvent}"/>
            <DataGridTextColumn Header="CarrierLoad" Binding="{Binding p_sTimeCarrierLoad}"/>
            <DataGridTextColumn Header="ProcessJobStart" Binding="{Binding p_sTimeProcessJobStart}"/>
            <DataGridTextColumn Header="ProcessJobEnd" Binding="{Binding p_sTimeProcessJobEnd}"/>
            <DataGridTextColumn Header="CarrierUnload" Binding="{Binding p_sTimeCarrierUnload}"/>
            <DataGridTextColumn Header="Data" Binding="{Binding p_sData}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
