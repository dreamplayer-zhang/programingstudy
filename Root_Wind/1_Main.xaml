<UserControl x:Class="Root_Wind._1_Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:MainConverter="clr-namespace:MainViewerConverter"
             mc:Ignorable="d"
             d:DesignHeight="840"
             d:DesignWidth="1912"
             Background="#FF2D2D30">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BorderBrush"
                         Color="Gray" />
        <SolidColorBrush x:Key="BackGround"
                         Color="#FF2D2D30" />
        <SolidColorBrush x:Key="BdrBackGround"
                         Color="White" />
        <SolidColorBrush x:Key="BdrTitleBackGround"
                         Color="#FF43437A" />
        <SolidColorBrush x:Key="TbForeGround"
                         Color="White" />
        <SolidColorBrush x:Key="TbBackGround"
                         Color="#FF919191" />
        <SolidColorBrush x:Key="TabBorderBrush"
                         Color="Gray" />
        <SolidColorBrush x:Key="TabBackGround"
                         Color="#FF222223" />
        <SolidColorBrush x:Key="TabSelected"
                         Color="#FF43437A" />
        <SolidColorBrush x:Key="TabMouseOver"
                         Color="#FF3E3E40" />
        <SolidColorBrush x:Key="TabForeGround"
                         Color="White" />
        <SolidColorBrush x:Key="BtnBorderBrush"
                         Color="Gray" />
        <SolidColorBrush x:Key="BtnBackGround"
                         Color="#FF222223" />
        <SolidColorBrush x:Key="BtnMouseOver"
                         Color="#FF3E3E40" />
        <SolidColorBrush x:Key="BtnPressed"
                         Color="#FF43437A" />
        <SolidColorBrush x:Key="BtnForeGround"
                         Color="White" />
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextBlock.FontFamily"
                    Value="Arial" />
            <Setter Property="TextBlock.Foreground"
                    Value="{StaticResource TbForeGround}" />
            <Setter Property="TextBlock.VerticalAlignment"
                    Value="Center" />
            <Setter Property="TextBlock.TextAlignment"
                    Value="Center" />
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border BorderBrush="{StaticResource BtnBorderBrush}"
                                Background="{StaticResource BtnBackGround}"
                                x:Name="Bdr">
                            <TextBlock FontSize="15"
                                       Foreground="{StaticResource BtnForeGround}"
                                       FontFamily="Arial"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center">
                                <ContentPresenter Margin="10" /></TextBlock>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Border.IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Bdr"
                                        Property="Background"
                                        Value="{StaticResource ResourceKey=BtnMouseOver}" />
                                <Setter Property="Cursor"
                                        Value="Hand" />
                            </Trigger>
                            <Trigger Property="Button.IsPressed"
                                     Value="True">
                                <Setter TargetName="Bdr"
                                        Property="Background"
                                        Value="{StaticResource ResourceKey=BtnPressed}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ToggleButton"
               x:Key="MyToggle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border CornerRadius="0"
                                Background="{StaticResource BtnBackGround}"
                                BorderBrush="{StaticResource BtnBorderBrush}"
                                BorderThickness="0"
                                x:Name="Bdr">
                            <TextBlock FontSize="15"
                                       FontFamily="Arial"
                                       Foreground="{StaticResource BtnForeGround}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center">
                                <ContentPresenter Margin="10" /></TextBlock>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Border.IsMouseOver"
                                     Value="True">
                                <!--#FF3E3E40-->
                                <Setter TargetName="Bdr"
                                        Property="Background"
                                        Value="{StaticResource ResourceKey=BtnMouseOver}" />
                                <Setter Property="Cursor"
                                        Value="Hand" />
                            </Trigger>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="Bdr"
                                        Property="Background"
                                        Value="{StaticResource ResourceKey=BtnPressed}" />
                            </Trigger>
                            <Trigger Property="IsChecked"
                                     Value="False">
                                <Setter TargetName="Bdr"
                                        Property="Background"
                                        Value="{StaticResource ResourceKey=BtnBackGround}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--<MainConverter:ProcessColorConverter x:Key="ProcessColorConverters"/>
        <MainConverter:StringToModuleStateConverter x:Key="StringToModuleStateConverters" />
        <MainConverter:ColorToModuleStateConverter x:Key="ColorToModuleStateConverters" />
        <MainConverter:VisibleLoadToLPStateConverter x:Key="VisibleLoadToLPStateConverters" />
        <MainConverter:ColorLoadToLPStateConverter x:Key="ColorLoadToLPStateConverters" />-->
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="87*" />
            <ColumnDefinition Width="13*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0"
                Margin="15"
                BorderThickness="3"
                BorderBrush="{StaticResource BorderBrush}">
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="25*" />
                    <ColumnDefinition Width="20*" />
                    <ColumnDefinition Width="20*" />
                    <ColumnDefinition Width="35*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0"
                      Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0"
                          Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderBrush="{StaticResource BorderBrush}"
                                Background="{StaticResource BdrTitleBackGround}"
                                BorderThickness="2">
                            <TextBlock Text="Load Port 1"
                                       FontSize="20"
                                       HorizontalAlignment="Left"
                                       Foreground="{StaticResource TbForeGround}"
                                       Margin="10,0,0,0" />
                        </Border>
                        <Border Grid.Row="1"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="2"></Border>
                    </Grid>
                    <Grid Grid.Row="1"
                          Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderBrush="{StaticResource BorderBrush}"
                                Background="{StaticResource BdrTitleBackGround}"
                                BorderThickness="2">
                            <TextBlock Text="Load Port 2"
                                       FontSize="20"
                                       HorizontalAlignment="Left"
                                       Foreground="{StaticResource TbForeGround}"
                                       Margin="10,0,0,0" />
                        </Border>
                        <Border Grid.Row="1"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="2"></Border>
                    </Grid>
                </Grid>
                <Grid Grid.Column="1"
                      Margin="15">
                    <Border BorderBrush="{StaticResource BorderBrush}"
                            Background="{StaticResource BackGround}"
                            BorderThickness="2">
                        <TextBlock Text="Load Port Info"
                                   Foreground="{StaticResource TbForeGround}"
                                   FontSize="20" />
                    </Border>
                </Grid>
                <Grid Grid.Column="2"
                      Margin="15">
                    <Border BorderBrush="{StaticResource BorderBrush}"
                            Background="{StaticResource BackGround}"
                            BorderThickness="2">
                        <TextBlock Text="Slot Info"
                                   Foreground="{StaticResource TbForeGround}"
                                   FontSize="20" />
                    </Border>
                </Grid>
                <Grid Grid.Column="3"
                      Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="7*" />
                    </Grid.RowDefinitions>
                    <Border BorderBrush="{StaticResource BorderBrush}"
                            Background="{StaticResource BackGround}"
                            BorderThickness="2">
                        <TextBlock Text="Job List"
                                   Foreground="{StaticResource TbForeGround}"
                                   FontSize="20" />
                    </Border>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="7*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                BorderBrush="{StaticResource BorderBrush}"
                                Background="{StaticResource BackGround}"
                                BorderThickness="2,0,0,2">
                            <StackPanel>
                                <!--<Label Content="Loadport1" Background="AliceBlue" HorizontalContentAlignment="Center"></Label>
                                <Label Content="{Binding p_Handler.p_loadport[0].p_eState, Converter={StaticResource StringToModuleStateConverters}}"
                                       Background="{Binding p_Handler.p_loadport[0].p_eState, Converter={StaticResource ColorToModuleStateConverters}}"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label Content="Loadport2"
                                       Background="AliceBlue"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label Content="{Binding p_Handler.p_loadport[1].p_eState, Converter={StaticResource StringToModuleStateConverters}}"
                                       Background="{Binding p_Handler.p_loadport[1].p_eState, Converter={StaticResource ColorToModuleStateConverters}}"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label Content="WTR"
                                       Background="AliceBlue"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label 
                                       Background="{Binding p_Handler.p_wtr.p_eState, Converter={StaticResource ColorToModuleStateConverters}}"
                                       Content="{Binding p_Handler.p_wtr.p_eState, Converter={StaticResource StringToModuleStateConverters}}"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label Content="Aligner"
                                       Background="AliceBlue"
                                       HorizontalContentAlignment="Center"></Label>
                                <Label Content="{Binding p_Handler.p_aligner.p_eState, Converter={StaticResource StringToModuleStateConverters}}"
                                       Background="{Binding p_Handler.p_aligner.p_eState, Converter={StaticResource ColorToModuleStateConverters}}"
                                       HorizontalContentAlignment="Center"></Label>-->
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="2">
                            <ListBox HorizontalContentAlignment="Stretch"
                                     BorderBrush="{x:Null}"
                                     Background="{StaticResource BackGround}"
                                     DataContext="{Binding p_Process}"
                                     ItemsSource="{Binding p_aFullSequence}"
                                     BorderThickness="2">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="Black"
                                                BorderThickness="1"
                                                HorizontalAlignment="Stretch">
                                            <Label Content="{Binding p_id}"
                                                   HorizontalAlignment="Stretch"
                                                   HorizontalContentAlignment="Center"
                                                   VerticalAlignment="Stretch"
                                                   Height="30">
                                                <!--<Label.Background>
                                                    -->
                                                <!--<MultiBinding Converter="{StaticResource ProcessColorConverters}">
                                                        <Binding />
                                                        <Binding Path="DataContext.p_aSequence"  UpdateSourceTrigger="PropertyChanged"
                                                                 RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBox}}" />
                                                    </MultiBinding>-->
                                                <!--
                                                </Label.Background>-->
                                            </Label>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
        <Grid Grid.Column="1">
            <StackPanel Margin="0,45,15,15">
                <Button Content="Start"
                        Margin="0,0,0,10" />
                <Button Content="Stop"
                        Margin="0,0,0,10" />
                <Button Content="Cancel"
                        Margin="0,0,0,10" />
                <Button Content="Pause"
                        Margin="0,0,0,10" />
                <Button Content="Initialization"
                        Command="{Binding InitializeCommand}"
                        Margin="0,0,0,10" />
                <Button Content="Recovery"
                        Margin="0,0,0,10" />
                <Button Content="Test"
                        Command="{Binding TestCommand}"
                        Margin="0,0,0,10" />
                <Button Content="Test"
                        Command="{Binding TestCommand}"
                        Margin="0,0,0,10" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
