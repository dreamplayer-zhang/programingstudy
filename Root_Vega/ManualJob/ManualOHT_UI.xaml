<Window x:Class="Root_Vega.ManualJob.ManualOHT_UI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Root_Vega.ManualJob"
        mc:Ignorable="d"
        Title="ManualOHT_UI" Height="700" Width="700" WindowStyle="ToolWindow" Background="#FF2D2D30" Topmost="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Header="Loadport A" Foreground="White" FontSize="15" Margin="8">
            <local:ManualOHTLoadport_UI x:Name="manualOHTA"/>
        </GroupBox>
        <GroupBox Grid.Column="1" Header="Loadport B" Foreground="White" FontSize="15" Margin="8">
            <local:ManualOHTLoadport_UI x:Name="manualOHTB"/>
        </GroupBox>
    </Grid>
</Window>
