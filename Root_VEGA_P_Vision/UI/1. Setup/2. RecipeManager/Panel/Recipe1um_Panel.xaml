<UserControl x:Class="Root_VEGA_P_Vision.Recipe1um_Panel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_VEGA_P_Vision"
             mc:Ignorable="d" 
             xmlns:Tools="clr-namespace:RootTools;assembly=RootTools"
             d:DesignHeight="836" d:DesignWidth="1912" 
             Background="{StaticResource PanelBackGround}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="12*" />
            <ColumnDefinition Width="88*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" BorderBrush="{StaticResource Splitter}"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="0,10,0,0">
                <Button Content="Snap" Style="{StaticResource SetupPanelBtn2}" />
                <Button Content="Inspection" Style="{StaticResource SetupPanelBtn2}" />
            </StackPanel>
        </Border>
        <Grid Grid.Column="1">
            <Grid>
                
            </Grid>
        </Grid>
    </Grid>
</UserControl>
