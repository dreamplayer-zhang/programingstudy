<UserControl x:Class="Root_VEGA_P_Vision.MaskTools_Panel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_VEGA_P_Vision"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource PanelBackGround}">
    <GroupBox Header="Mask Tools">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <UniformGrid Rows="1" Grid.Row="0" >
                <RadioButton IsChecked="{Binding IsDraw}" Command="{Binding btnDraw}">Draw</RadioButton>
                <RadioButton IsChecked="{Binding IsDraw,Converter={StaticResource InverseBoolean}}" Command="{Binding btnErase}">Erase</RadioButton>
            </UniformGrid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <GroupBox Header="Thickeness" Grid.Column="0">
                    <ComboBox HorizontalContentAlignment="Left"
                                                      SelectedIndex="{Binding p_nThickness}">
                        <ComboBoxItem Content="0" Visibility="Collapsed"/>
                        <ComboBoxItem Content="1" />
                        <ComboBoxItem Content="2" />
                        <ComboBoxItem Content="3" />
                        <ComboBoxItem Content="4" />
                        <ComboBoxItem Content="5" />
                        <ComboBoxItem Content="6" />
                        <ComboBoxItem Content="7" />
                        <ComboBoxItem Content="8" />
                        <ComboBoxItem Content="9" />
                        <ComboBoxItem Content="10" />
                        <ComboBoxItem Content="11" />
                        <ComboBoxItem Content="12" />
                        <ComboBoxItem Content="13" />
                        <ComboBoxItem Content="14" />
                        <ComboBoxItem Content="15" />
                    </ComboBox>
                </GroupBox>
                <GroupBox Header="Threshold" Grid.Column="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="7*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding p_nThreshold}"/>
                        <ComboBox Grid.Column="1" SelectedIndex="{Binding p_nselectedUpdown}">
                            <ComboBoxItem Content="Up" />
                            <ComboBoxItem Content="Down" />
                        </ComboBox>
                    </Grid>
                </GroupBox>
            </Grid>
            <UniformGrid Rows="1" Grid.Row="2">
                <Button Content="Pen" Command="{Binding btnPen}"/>
                <Button Content="Rect" Command="{Binding btnRect}"/>
                <Button Content="Threshold" Command="{Binding btnThreshold}"/>
            </UniformGrid>
            
        </Grid>
    </GroupBox>
</UserControl>
