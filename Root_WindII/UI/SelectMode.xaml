<UserControl x:Class="Root_WindII.SelectMode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_WindII"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="Setup" Command="{Binding btnSetupCommand}" FontSize="40" FontWeight="Bold" FontFamily="Arial"/>
        <Button Grid.Column="1" Command="{Binding btnReviewCommand}" Content="Review" FontSize="40" FontWeight="Bold" FontFamily="Arial"/>
        <Button Grid.Column="2" Command="{Binding btnOperationCommand}" Content="Operation" FontSize="40" FontWeight="Bold" FontFamily="Arial"/>
    </Grid>
</UserControl>
