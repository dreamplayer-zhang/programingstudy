<UserControl x:Class="Root_WindII.RACProduct"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:tools="clr-namespace:RootTools;assembly=RootTools"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_WindII"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="MapFileListViewerButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource groupbox_button_normal}"/>
            <Setter Property="Foreground" Value="{StaticResource groupbox_button_text}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border BorderThickness="0" 
                                Width="{TemplateBinding Width}"
                                Height="48"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource sidetap_parent_menu_text}"
                                HorizontalAlignment="{TemplateBinding HorizontalAlignment}">
                            <ContentPresenter  VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                               HorizontalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger  Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource groupbox_button_mouseover}"/>
                            </Trigger>
                            <Trigger  Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource groupbox_button_press}"/>
                                <Setter Property="Foreground" Value="{StaticResource groupbox_button_press_text}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="{StaticResource main_view_background}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Margin="4"
                  Header="ProductID(XML) List" Style="{StaticResource main_view_groupbox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="9*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <local:MapFileSelectionViewer Grid.Column="0" DataContext="{Binding MapFileSelectionViewerVM}"/>
                <StackPanel Grid.Column="1"
                            Orientation="Vertical">
                    <Button Content="Open" Style="{StaticResource MapFileListViewerButton}" Command="{Binding CmdOpen}" Margin="0,4,4 4"/>
                    <Button Content="New" Style="{StaticResource MapFileListViewerButton}" Command="{Binding CmdNew}" Margin="0,0,4,4"/>
                    <Button Content="Save" Style="{StaticResource MapFileListViewerButton}" Command="{Binding CmdSave}" Margin="0,0,4,4"/>
                    <Button Content="Delete" Style="{StaticResource MapFileListViewerButton}" Margin="0,0,4,4"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <Grid Grid.Column="1" >
            <ListBox Margin="4"
                     ItemsSource="{Binding ModuleList}"
                     SelectedIndex="{Binding p_SelectedChipIndex}"
                     HorizontalContentAlignment="Stretch"
                     Background="Transparent">
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
