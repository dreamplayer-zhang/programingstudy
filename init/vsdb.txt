//규칙. 첫단어는 TABLE 이름. ()안은 데이터 타입,
// Index로 쓸꺼는 앞에 * 붙이기
// Primary key로 쓸꺼는 앞에 @붙이기
Datainfo,LotIndexID(INTEGER),InspStartTime(TEXT),BCRID(TEXT),RCPID(TEXT),DataSaveTime(TEXT),DefectCount(INTEGER)
Data,@No(INTEGER),DCode(INTEGER),Size(INTEGER),Length(INTEGER),Width(INTEGER),Height(INTEGER),InspMode(INTEGER),FOV(INTEGER),PosX(INTEGER),PosY(INTEGER)