<UserControl x:Class="Root_VEGA_P.NozzleItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Root_VEGA_P"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Padding="5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.8*"/>
        </Grid.ColumnDefinitions>
        <CheckBox HorizontalAlignment="Center" Grid.Row="0" IsChecked="{Binding IsEnabled}"/>
        <TextBlock HorizontalAlignment="Center" Grid.Column="1" Text="{Binding Num}"/>
        <TextBlock HorizontalAlignment="Center" Grid.Column="2" Text="{Binding Pressure}"/>
        <TextBlock HorizontalAlignment="Center"  Grid.Column="3" Text="{Binding Sec}"/>
    </Grid>
</UserControl>
