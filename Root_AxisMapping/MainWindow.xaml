<Window x:Class="Root_AxisMapping.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Root_AxisMapping"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Title="AxisMapping" Height="1000" Width="1800" WindowState="Maximized">
    <Window.Resources>
        <DataTemplate x:Key="TabItemHeaderTemplate">
            <ContentPresenter Content="{TemplateBinding Content}" RenderTransformOrigin="0.5,0.5">
                <ContentPresenter.LayoutTransform>
                    <RotateTransform Angle="-90"/>
                </ContentPresenter.LayoutTransform>
            </ContentPresenter>
        </DataTemplate>
    </Window.Resources>
    <TabControl TabStripPlacement="Left">
        <TabItem Header=" AxisMapping " HeaderTemplate="{DynamicResource TabItemHeaderTemplate}">
            <local:Mapping_UI x:Name="mappingUI"/>
        </TabItem>
        <TabItem Header=" Engineer " HeaderTemplate="{DynamicResource TabItemHeaderTemplate}">
            <local:AxisMapping_Engineer_UI x:Name="engineerUI"/>
        </TabItem>
    </TabControl>
</Window>
